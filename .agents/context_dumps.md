# External Context Dumps

Archived full agent conversations live **outside** this repo (also not committed).

## Current archive

**`<EXTERNAL_CONTEXT_DUMPS_ROOT>/<DATE>/`**

| Session | ID | Folder |
|---------|-----|--------|
| Agent session | `<SESSION_ID>` | `<SESSION_FOLDER>/` |
| Agent session | `<SESSION_ID>` | `<SESSION_FOLDER>/` |

Start with each folder's `RESUME_STATE.md` or `INDEX.md` at the dump root.

## When to consult

- Recovering interrupted work after rate limits or IDE crashes.
- Understanding decisions made in a prior agent session.
- Cross-checking journal entries against full transcripts.

The **dev journal** (`journal.md`) is the canonical in-repo summary; dumps are supplementary archaeology.
# Copy to .env (gitignored) for local server/UI development. Never commit real values.

# llama-server API key (Bearer / X-Api-Key)
LLAMA_API_KEY=

# Hugging Face hub downloads
HF_TOKEN=

# Optional: Web UI dev (tools/ui)
VITE_PUBLIC_SERVER_ORIGIN=http://localhost:8090
VITE_DEBUG=false
